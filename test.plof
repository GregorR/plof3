var a = Object : [ a = psl { 3 integer }; ];
var b = a : [];
psl {
    plof{b.a} pul_eval print
};

True.ifTrue(psl { "GOOD " print });
False.ifTrue(psl { "BAD" print });

if (True) (
    psl { "GOOD " print }
) else (
    psl { "BAD" print }
);

var curryish = (a, b, c) {
    psl {
        plof{a} pul_eval print
        plof{b} pul_eval print
        plof{c} pul_eval print
    };
};

curryish psl{"FOO"} psl{"BAR"} psl{"BAF"};
