This is Plof, an extremely dynamic programming language. Included here is
cplof, a C implementation of Plof, and Plof Core, the standard library for Plof
(which should be compatible with any Plof implementation).
