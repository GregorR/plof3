#!/bin/bash
cd cplof

# makefile-based tests
runtest make_noauto_Wall make -k -f build/Makefile.gcc ECFLAGS="-ansi -Wall -Werror -D_BSD_SOURCE"
make -f build/Makefile.gcc clean
runtest make_noauto make -k -f build/Makefile.gcc
make -f build/Makefile.gcc clean

cd ..
