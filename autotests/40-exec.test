#!/bin/bash
for i in autotests/exec/*
do
    bni=`basename $i`

    pushd $i
    
    # Now run it
    runtest exec_${bni}_run trybtout output cplof *.plof
    runtest exec_${bni}_cmp cmp output expected
    rm -f output

    popd
done
