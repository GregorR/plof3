var print = (x) {
    psl { this "x" pul_var member pul_eval print null }
};
var libc = C.dlopen("libc.so.6");
var get_nprocs = C.dlsym(libc, "get_nprocs");
var cif = C.prepcif(C.int, array(), 0);
var ret = C.call(cif, get_nprocs, array());
print(ret.__value);
